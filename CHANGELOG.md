# 开源文档整理（0.19.1）

- 重写仓库主页，记录 Ripple × GPT 共同制作、GPT 主刀实现、Fable5.1 参与讨论的分工，以及桌宠建议和“糯叽”名字的由来。
- 说明 MIT 授权下允许替换形象、二改、增加功能和分发修改版；补充素材尺寸、代码入口及验证说明。
- 清理过时的喂食计划、安装占位和常驻气泡描述，记录 Ripple 的实际使用验收反馈。
- 仅修改文档，运行版本保持 0.19.1。

# 0.19.1 — 气泡跟紧糯叽

- 修复靠近屏幕顶部时气泡翻到身体中下方：优先在头顶，上方不足时放在有空间的一侧，两侧均不足才放在完整身体下方。
- 气泡小尾巴随方向与屏幕边缘调整，拖动时持续跟随；尺寸包含内边距，避免长文案越界。
- 顶部两侧、底部回退、普通头顶和移动端视口偏移的定位测试通过；手机酒馆视觉效果待真机验收。

# 0.19.0 — 更小、更安静、记得每张卡

- 最小尺寸从 70% 降至 40%，滑块和保存校验同步调整，已有尺寸不强制改变。
- 移除当前聊天角色回复总数及变量，不再为播报扫描聊天历史；保留今日卡数和新增回复计数。
- 旧版自定义台词中含退役楼层变量的句子自动清理，原文先备份。
- 新增安静 / 日常陪伴：安静时只回应主动互动；日常主动气泡至少间隔 10 秒，每次最多 5 秒。
- 新增按角色卡、按场景的专属台词，留空继承通用；编辑器随角色切换刷新，群聊使用通用台词。
- 计数、控制器与编辑器模拟测试通过；动画渲染器和素材未改，手机酒馆界面待真机验收。

# 0.18.1 — 播报气泡轻一点

- 长按及设置按钮触发的统计播报改为出现后 5 秒自动消失，不再停留 16 秒；长文案也不会延长播报时间。
- 保留松手后继续阅读的行为，其他气泡、统计和动画不变。
- 增加 4999 ms 仍显示、5000 ms 准时关闭的控制器回归检查。

# 0.18.0 — 有记性的陪伴猫

- 新增按场景自定义气泡：多句随机、变量替换、预览和恢复默认，纯文本显示并自动保存。
- 按设备当地时间问候与播报；长按显示今日卡数、新增角色回复数和当前聊天角色回复总数，松手后仍可阅读 16 秒。
- 今日统计随账户扩展设置保存，跨当地午夜归零；排除历史加载、重生成、续写、候选切换及非角色回复，支持群聊角色去重。
- 统计从启用后开始，关闭糯叽时暂停；只保存当天去重记录，不保存聊天正文。
- 清理喂食文案；现有动画素材与渲染器保持不变。
- 长气泡自动换行并约束在可见视口内；增加键盘 Shift + Enter / 空格播报入口。
- 计数与控制器测试通过（含跨日、刷新、群聊去重、长按松手与关闭暂停）；浏览器预览受环境访问策略限制，手机酒馆真机界面仍待验收。

# 0.17.1 — 修复上传损坏的身体素材

- 恢复上传时被截断的身体 PNG 及其他受影响的素材、预览文件；上传后逐一校验 Git blob 哈希。
- 身体图片增加版本参数，避免继续使用浏览器缓存的损坏图片。
- 不改动已验收的耳朵与歪头 wink 动作。

# 0.17.0 — 歪头 wink 打招呼

- 将探爪替换为歪头、单眼 wink、睁眼、回正，完整动作 2.8 秒；腿和爪子保持原坐姿。
- 复用已验收的闭眼素材，加载时缓存单眼羽化遮罩，仅覆盖画面左侧眼睛；另一只眼睛与鼻子保持原样。
- 眼皮、脸和耳朵共享头部变换；脖子平滑衔接，设计坐标 y=300 以下保持固定。
- 保留已验收的听觉耳朵动作。移除旧探爪的运行时加载和绘制，紧急矢量外观也不再抬爪。
- 按钮改名“歪头打招呼”，保留内部 wave 状态及既有接口；减少动态效果时保持静态 wink。
- 验证单眼覆盖范围、固定腿爪、额头不随听觉动作移动、动作衔接与控制器中断。120 帧原生 Canvas 预览包含脸部特写；尚未在手机酒馆真机验收。

# 0.16.0 — 探爪碰你

- 将大幅度挥爪替换为 3 秒的小幅探爪：轻轻离地、向外探一下、停留、收回。
- 整条前腿与爪子使用同一张完整纹理做小角度旋转，去掉分段压缩、前臂换图和单独转爪，肢体各部分尺寸保持不变。
- 起落末端恢复原坐姿身体贴图，减轻补毛区域切换；首次加载只需身体与完整前腿两张 PNG。
- 设置与预览入口改名“探爪碰你”；内部 wave 状态和已有调用接口保留，计时统一为 3 秒。
- 保留用户已验收的耳朵；验证固定额头、完整肢体尺寸、支撑爪落点、连续轨迹和控制器中断。原生 Canvas 动图已生成，尚未在手机酒馆真机验收。

# 0.15.0 — 固定耳根与分离小爪

- 耳根下的毛色与花纹保持原位，耳尖旋转、耳根渐变弯曲；听你说话时取消整头倾斜。
- 挥爪延长到 3.7 秒，由肘部、前臂角度与长度推导手腕位置，前臂长度保持在静止姿态的 95% 以上。
- 圆爪单独绘制一份，前臂贴图移除重复爪子；修剪腕部突出块，放大身体补毛范围以清理旧腿边线。
- 新增本体与红蓝骨架同步预览：红色上臂、蓝色前臂、金色爪子，便于检查连接与走向。
- 验证：固定额头像素、前臂长度、支撑爪落点、动作连续性及控制器中断，保留走路与蹭蹭回归。预览使用原生 Canvas，尚未在手机酒馆真机验收。

# 0.14.0 — 耳朵完整拆层与自然抬爪

- 清理身体底图里的旧耳尖和耳缘，补绘耳根下的头顶；完整耳廓随耳朵一起转动。
- 挥爪改为收爪、抬到胸前、向外轻招两下、收回、落地，共 3.2 秒。
- 新增蓬松的折叠前臂与自然下垂圆爪素材，用共同肘部和爪心坐标衔接站立与抬爪纹理。
- 上下臂改用完整纹理和柔和关节遮罩，移除弯曲切片造成的条纹与裂缝。
- 身体轻微重心转移，脸、耳与闭眼层一起移动，支撑爪保持贴地。
- 验证：动作连续性与控制器计时、耳尖残留与原眼鼻像素、走路/蹭蹭回归、原生 Canvas 全周期渲染。尚未在 iPhone / SillyTavern 真机验收。

# 更新记录

## 0.13.0

- “我在听”新增明显的竖耳和左右错拍转耳，配合轻微偏头；沿用固定前爪的头颈模型，眼睛与耳朵跟随头部
- “挥爪”新增真正的单前爪抬起、弯肘招手两次、平稳落回；肩部固定，肘部用连续弧线变形，另一只前爪保持承重
- 使用 ImageGen 补出抬爪后的胸腹毛，仅将对应区域用于新图层；脸、耳朵、承重前爪与原始 PNG 素材保留
- 新前腿和补毛身体预先输出为 PNG，运行时按需加载，不再现场合成或处理像素；载入失败沿用既有姿态，销毁后忽略迟到的载入结果
- 挥爪统一至少 2.4 秒，包含设置、欢迎与拖动结束等入口；独立预览播完回到陪伴，再次触发可重播
- 新增 tools/build-wave-assets.mjs 和 tools/test-gestures.mjs；保留 0.12.0 蹭蹭与 0.11.0 行走检查

## 0.12.0

- 新增独立 `nuzzling` 蹭蹭状态：偏头凑近、闭眼蹭两下、舒展坐好，一次 4.2 秒；头部刚性转动、颈胸柔性跟随、前爪固定，耳朵与闭眼贴片随头移动
- 直接复用现有 PNG 图层绘制，尾巴保持独立枢轴，不新增模型图片或聊天请求
- 双击改为蹭蹭，保留单击摸摸、长按呼噜；设置与独立预览新增“蹭蹭你”，公开接口新增 `window.NuojiPet.nuzzle()`
- 动作结束回到待机；重复触发可重播，打字、拖动与生成沿用原优先级中断，减少动态效果时保持静止贴贴姿势
- 坐姿基准下移 24 个设计像素，为耳尖留出上方余量，并让坐姿落点更接近趴姿与影子
- 新增 `tools/test-nuzzle.mjs`，检查动作曲线、脸型、固定爪子、状态计时、重新触发及输入/拖动/生成中断
- 0.11.0 行走模型与全部 PNG 素材保持不变

## 0.11.0

- 行走改为固定肩髋的柔性分层变形：腿根留在原位置，大腿逐渐形变，小腿和爪子跟随落点，取消运行时整腿下放补偿
- 四只脚共用按路程计算的步幅；承重阶段的水平位移与页面移动抵消，摆动阶段平滑抬爪、前送和落地
- 身体的换重心、微倾和呼吸式压缩只作用于头身尾，不再带着承重脚离地
- 步幅按画布显示尺寸换算，移除旧的最小 24 px 限制，避免小尺寸下再次滑步
- 独立预览补上步态推进，并加入暂停步态、进度拖动，方便检查左右镜像与腿根
- 保留原有全部 PNG 素材与其他姿态；旧步态计算导出暂留兼容，新绘制使用固定根部模型
- 新增 tools/test-walk.mjs（Node 原生断言，无第三方依赖）和 docs/walk-preview.gif

## 0.10.4

- 被别的面板压在下面时不再抢触控：命中检测先问浏览器这个点最上层的元素是不是糯叽自己，抽屉、弹窗、扩展管理这些叠在她上面的东西现在能正常点到；她露在外面的部分照旧可以摸、拖、长按

## 0.10.3

- 触控更让路：只有明确画上的像素才算糯叽（透明度阈值 40 → 72），手指命中的容错圈从 14 px 缩到 8 px，紧挨着她的按钮不会再被毛尖吞掉
- 点击守卫只拦浏览器紧随其后合成的那一次 click：时长 900 → 450 ms，半径 36 → 24 px，拖完糯叽马上点别处不再被吃掉
- 补上按压的兜底：最后一根手指离屏、切后台、窗口失焦、或按住超过 15 秒没有任何指针事件，都会释放按压状态。之前 pointerup 一旦被系统手势吞掉，整页触控会一直被拦到刷新为止

## 0.10.2

- 修掉“漏屁股”：远侧后腿合成大腿柱的顶端原本比臀部轮廓还靠后约 30 px，放大后从屁股后面探出一截。现在柱子上端朝前收拢，后缘贴着臀部轮廓藏进毛里，身体以下的可见大腿粗细不变
- 两条近腿的关节帽在身体遮盖线以下多出来的那圈毛，按各自的真实腿形裁掉并羽化；关节帽只在身体之下继续补缝
- `tools/split_front_paw.py` 新增 `trim_cap`，需要 v2 近腿作为轮廓参考，两张源图重新入库

## 0.10.1

- 行走时头身尾母版整体放大 6% 并下沉 8 px，以上背为锚点：胸腹毛压住更多腿根，身腿比例更协调，爪子与地线位置不变
- 设置新增“让糯叽自己散步”开关，关掉后不再自动巡游；“糯叽走两步”按钮照常可用
- 四条腿、尾巴摆动、接地补偿与其他形态保持不变

## 0.10.0

- 修复：设置面板的层级刷新会顺手清掉“生成结束等回信”的 400 ms 定时器；出错时只要页面滚动或点击一下，糯叽就会一直缩成团子举着“让我想想…”。现在两者互不相干
- 修复：“扮演我说”（impersonate）从不发 MESSAGE_RECEIVED，之前每次用完都会被当成报错。现在它有自己的气泡“帮你想想怎么说～”，完成时听 IMPERSONATE_READY 或 GENERATION_ENDED 收尾，说“帮你写好啦～”
- 修复：生成看门狗从 180 秒放宽到 300 秒，并且每收到一个流式 token 就重新计时；即便真的超时，晚到的回信也照样迎接，不再被忽略
- 行走时尾巴改为独立图层，以尾根为轴：叠加坐姿的心情摆尾与随步幅的慢滚，大尾巴一路走一路摆。`tools/split_walk_tail.py` 负责从身体母版切出尾巴
- 删除 v0.3.6 起就不再调用的 DOM / 按钮 / 聊天观察器降级代码（约 170 行），以及从未加载的 16 张历史素材（约 3.8 MB）；重建脚本所需的 v2 远腿与 v4 近腿源图保留。旧文件仍可在 v0.9.11 的提交历史里找到
- `window.NuojiPet.status()` 的 `generationControlActive` 字段改为 `awaitingLateReply`

## 0.9.11

- 近侧前爪的脚掌拆成独立一层，以手腕为轴：承重时压平贴地，摆动到最高点时蜷回母版里悬着的姿势；落地不再像踮脚
- 近侧前腿落地时的伸直量从 12% 降到 5%，肘部保留弯度，其余高度差由整体下放补足，接地点按压平后的脚掌重新测量
- 两条远腿的小腿与爪子整体加宽 18%，大腿柱顶宽同步加到 1.8 / 2.7 倍；两条近腿加宽 8%；四条腿粗细更协调
- 新增 `tools/split_front_paw.py`，负责近腿加宽与脚掌拆分；`tools/rebuild_far_legs.py` 增加 `widen` 参数
- 步态曲线、接地补偿、绘制顺序、趴姿、团子、闭眼、耳尾、气泡及输入/生成联动保持不变

## 0.9.10

- 四只爪子都真正踩地：为每条腿记录爪垫相对肩/髋枢轴的接地点与地线，支撑期先把腿沿枢轴微微伸直、再把余量整体下放，爪底始终贴在地线上；刚性旋转带来的"弧线滑步上浮"被完全抵消
- 近侧前腿母版本来是抬起的姿势，绕肩膀怎么转都够不到地面，这是"小前爪踩不实"的根源；现在它落地时伸直 12%、其余靠下放，前摆角度略收，四爪同高
- 摆动期抬爪峰值前移到中段偏前，后半程用来向前探爪、缓缓落下，落地相保留；爪子从地线抬起、再回到地线，不再从半空硬接支撑期
- 新增导出的 `getWalkGroundContact()`，接地补偿可以脱离浏览器做回归测试
- 远腿 v3、绘制顺序、趴姿、团子、闭眼、耳尾、气泡及输入/生成联动保持不变

## 0.9.9

- 行走绘制顺序改为“远腿 → 近腿 → 完整无腿身体”：四条完整腿先全部画好，整张身体最后压上去，由真正的胸腹毛担任唯一遮罩；身体之上不再叠任何东西，关节帽和腿根都藏在毛下面
- 停用 0.9.8 的腹毛轮廓层：完整身体本身就是遮罩，不再需要额外裁一条腹毛；文件保留但运行时不加载
- 真机截图里的“断腿”真凶是两条远侧腿：旧 v2 素材把肩/髋上方裁成一整块平行四边形，里面复制了近侧腿的大腿毛；近腿一迈开，这块方形毛片就连同一条水平台阶暴露出来。远腿升级为 v3：小腿与爪子逐像素保留，上方改为从小腿毛发合成、朝肩/髋枢轴延伸的大腿柱，全部收在身体之下
- 合成脚本 `tools/rebuild_far_legs.py` 一并入库，日后重抠母版可直接重跑
- 步态曲线、落地相、趴姿、团子、闭眼、耳尾、气泡及输入/生成联动保持不变

## 0.9.8

- 按小狐狸画出的正确遮挡关系重排腿根：四条完整活动腿伸进身体，最后由取自“无腿身体母版”的腹毛轮廓层盖住关节开口
- 新腹毛层只含肚皮下沿与原生毛尖，不含任何固定腿像素；腿摆到最开时也不会露黑缝，更不会重新长出第五条腿根
- 小前爪回摆末段增加一次短促的落地相：提前降爪、踩实，再进入承重后划，补齐 24 fps 下缺失的最后一拍
- 趴姿、团子、困困闭眼、耳尾、气泡及输入/生成联动保持不变

## 0.9.7

- 重做两条近侧腿的根部权重：取消 138 px 的纵向大渐隐，改成围绕肩/髋枢轴、随腿一起旋转的柔和关节帽
- 关节帽使用原侧身母版的同位置毛发并在四周羽化；腿摆到最大角度时仍与身体保持宽实连接，不再出现“悬浮义肢”
- 前腿最薄相位的实心重叠面积提升到旧版约 3.4 倍，同时继续停用会滞留原地的固定 `underpaint`
- 趴姿、团子、耳尾、气泡、输入与生成联动保持不变

## 0.9.6

- 移除行走时固定在腹部下方的 `underpaint` 补毛带；它不会随腿旋转，是抬爪时出现“多一截腿根”的真正原因
- 行走顺序收敛为“远腿 → 完整身体 → 柔边近腿”，四条腿均围绕自己的关节连续运动，不再留下静止假关节
- 八个步态相位由测试脚本直接注入，不依赖浏览器的 `prefers-reduced-motion`，确保检查到的不是四张相同静止图
- 保留 0.9.5 的趴姿去绿、闭眼贴片、完整后腿，以及全部事件联动与 190 ms 单通道转场

## 0.9.5

- 趴姿改用预先清净绿幕的透明母版，耳尖、耳后和颈毛不再残留荧光绿边；首次趴下也省去整张图片的手机端像素处理
- 闭眼贴片收紧为只覆盖双眼，并单独清理羽化区里的绿幕像素；困困嘟闭眼时不会再把耳后荧光绿盖回来
- 行走图层顺序改为“远腿 → 肩髋底毛 → 身体 → 柔边近腿”，小前腿从胸毛自然长出，不再出现纸片分层线
- 柔边近侧后腿移到身体之上，透明腿根继续藏在腹毛里，但大腿和小腿不再被身体遮掉一半
- 同步收紧团子及行走降级图的运行时绿幕阈值；其余步态、转场、表情与事件联动保持不变

## 0.9.4

- 逐层重抠行走身体、四腿与肩髋底毛，收紧绿幕边缘阈值，清除肚子下方随步态闪动的黄绿色毛边
- 身体、远腿、近腿与底毛分别升级为 v2 / v2 / v3 / v2；银白与暖粉毛色保持不变，只处理绿幕污染像素
- 保留 0.9.3 的无双影收身转场、0.9.2 的腿根柔边与困困闭眼，步态和所有事件联动均未修改
- 团子继续只在思考时咕噜噜，糯叽没有被迫转职成全职保龄球

## 0.9.3

- 修掉坐姿与行走姿态之间约 460 ms 的整猫交叉淡化；两张轮廓不再同时出现，不会在录屏里变成半透明“双胞胎糯叽”
- 行走切换改为约 190 ms 的单通道收身转场：旧姿态先轻轻伏低淡去，到中点才换成新姿态展开
- 切换最低透明度保留 12%，避免中点整只消失；同时用脚底为锚点压低身体，减少不同体型母版之间的瞬移感
- 只调整坐姿 ↔ 行走的进出场，不修改四拍步态、腿根柔边、困困闭眼及其他形态之间原有的柔和过渡

## 0.9.2

- 两条近侧腿升级为柔边 v2：只在肩口与髋口内渐隐裁切边，保留小腿、爪子与全部步态曲线不变
- 原图底毛继续承接腿根，消除 150% 放大时仍可见的两处纸片分层线
- 为独立趴姿增加专属闭眼小层；“困嘟嘟”现在会真正温柔闭眼，不再睁眼冒 `Z`
- 闭眼层只覆盖双眼附近，趴姿的脸型、身体、前爪和大尾巴仍逐像素复用原母版，避免整张换图漂移
- 保留 0.9.1 的四拍慢走、72% 支撑期、短步幅与肩髋底毛层

## 0.9.1

- 把对角腿完全同步的两拍小跑改为四拍慢走：同侧后爪、同侧前爪、另一侧后爪、另一侧前爪依次落地
- 对角腿保留轻微错拍的呼应但不再同起同落，前后腿在腹下自然交叉，避免四条腿像各自接了马达
- 每只爪的支撑期增至步态周期约 72%，摆动期改为平滑抬爪、收腿、前送、落地，慢走时大部分时间保持三爪承重
- 页面步幅由约 0.52 个身长缩到 0.24 个身长，并将默认巡游放慢到 3.2 秒，让落地爪与页面位移更接近、不再明显滑步
- 身体改成四次极轻压步与两次肩胯换重心；动作留在脚下，不再整块头身尾上下弹跳
- 新增只覆盖肩口与髋口的同尺寸原图底毛层，按“远腿 → 底毛 → 近腿 → 身体”绘制，补掉放大时可见的两处透明断口
- 不更换任何行走皮肤或分层素材，固定脸、身体、耳朵与尾巴继续保持零换帧漂移

## 0.9.0

- 行走机制从两张整猫图硬切改为一张固定头身尾层与四条独立透明腿层，彻底消除换帧时的脸、耳、身长和尾巴跳动
- 四条腿分别绕肩/髋枢轴连续旋转；前近腿+后远腿与前远腿+后近腿分成相差半周期的对角线组
- 每条腿按连续正弦曲线完成前摆、抬脚、落地与后蹬，不再只在两个劈腿姿势之间瞬移
- 步态相位改由实际走过的 CSS 像素除以步幅计算，与 2300 ms 巡游位移使用同一进度；走速变化时不再滑步
- 身体起伏绑定同一相位，仅在经过相轻抬 2 px；启动和减速时腿速也会随实际位移自然变化
- 五张透明层仍在首次巡游时懒加载；任一分层素材失败时自动回退到原侧身母版，不影响其他状态
- 保留 v0.8.x 的安全边界和中断机制、v0.7.0 的生成团子、v0.6.0 的趴姿，以及已修稳的流式 / 非流式生命周期

## 0.8.2

- 修复第二步态中最靠近尾巴的后脚始终踩在同一位置、看起来像被钉在地上的问题
- 该后脚现在会在交替帧里完整离地并向后蹬，四只爪全部参与步态循环
- 只重绘漏动的后腿，保持糯叽的脸、身体、另外三只爪、大尾巴、画布位置与巡游逻辑不变

## 0.8.1

- 修复侧身巡游时四只爪子保持不动、整只糯叽像贴纸漂移的问题
- 新增相反落爪的第二步态母版：左右前爪与后爪按对角线交替抬起、落地和后蹬
- 行走时以约 4.6 帧/秒切换两组清晰落爪姿势，保留轻量 Canvas 路线，不引入序列图播放器
- 身体上下起伏从 5.2 px 降到 1.6 px，倾斜与压缩同步减弱，让视觉重心留在脚掌而不是整猫悬浮
- 两张步态图仍只在第一次走路时懒加载并抠除绿底；任一帧加载失败时自动复用另一帧，不影响巡游

## 0.8.0

- 新增第四形态“侧身小步”：保持糯叽的银白长毛、琥珀眼、尖耳、纤细身形与蓬松大尾巴
- 同一张生产母版由 Canvas 实时左右镜像，转向不会换脸；用轻微抬落、压缩与前后摆动表现步伐，避免多帧生图漂移
- 待机时每隔约 42–78 秒偶尔巡游一次，每次只走约 52–92 px，并按视口剩余空间自动选择安全方向
- 走到边缘会自动折返，结束后保存新位置并坐下；输入、生成、触摸、拖动、切换聊天和其他动作均可立即打断
- 开启“减少动态效果”后停用主动巡游；新增设置按钮“糯叽走两步”和 `window.NuojiPet.walk(direction, duration)` 调试入口
- 行走素材继续按需懒加载、首次抠除纯色底后缓存；不增加酒馆初次启动的图片处理负担
- 保留 v0.7.0 的生成团子、v0.6.0 的自动趴下、v0.5.0 的输入注视与手势，以及已修稳的流式 / 非流式生命周期

## 0.7.0

- 新增第三形态“糯叽团”：身体保持纤细，大尾巴从外圈将自己包成银白圆团，不复活保龄球体型
- 正式 `THINKING` 状态自动从坐姿或趴姿收成团子；回复、停止与异常结束时解团并衔接原有状态
- 团子在流式输出期间围绕自身中心贴地轻滚、轻跳和轻微压缩；`STREAM_TOKEN_RECEIVED` 沿用原脉冲信号增加短促滚动推力
- 两形态混合器升级为坐姿 / 趴姿 / 团子三权重过渡，可在任意两种形态之间约 460 ms 平滑切换
- 趴姿与团子改为按需懒加载：待机准备趴下或首次生成时才解码、抠除纯色底并缓存，避免增加酒馆启动和设置展开负担
- 新增 `window.NuojiPet.rollUp()` 调试入口；“想一想”预览同步展示团子形态
- 保留 v0.6.0 的自动趴下、v0.5.0 的输入注视与手势，以及 Fable51 修稳的流式 / 非流式生命周期

## 0.6.0

- 加入第一套新形态：保持银白毛、琥珀眼、粉鼻、长耳与蓬松大尾巴的自然趴姿
- 建立可扩展的坐姿 / 趴姿形态状态机，以约 460 ms 的压低、缩放和淡换完成过渡，不硬拉原坐姿变形
- 待机 28 秒后自动趴下；触摸、拖动、打字、生成和多数互动状态会立即唤醒并坐起
- “困嘟嘟”状态直接切换趴姿；提供 `window.NuojiPet.lieDown()` / `sitUp()` 供调试和未来剧情联动
- 生成器透明导出异常时采用纯色生产母版，图片载入后只做一次绿幕抠除并缓存，聊天绘制循环不重复处理像素
- 趴姿未加载或处理失败时继续使用稳定坐姿；不修改 v0.5.0 的生成生命周期、输入注视和三种手势

## 0.5.0

- 输入 `#send_textarea` 时立即进入倾头竖耳的注视状态，停笔 3 秒后回到待机；只做视觉陪伴，不干预已修稳的生成生命周期
- 思考满 20 秒与 40 秒分别更新常驻气泡为“这次想得久哦～”和“还在想呢…”，回复、停止、异常、切换聊天及销毁时完整清理计时器
- 点击手势拆为三种：单击摸摸、双击开心翻肚皮、长按 600 ms 持续呼噜并在松手后结束
- 长按与拖动互斥，移动超过阈值即取消长按；阻止猫身上的系统长按菜单，同时继续保留透明像素点击穿透
- 不新增皮肤素材、不重画眼睛、不修改 Fable51 修稳的流式 / 非流式事件顺序与手机性能限制

## 0.4.2

- 放大手机端的小耳朵动作：听你说时快速竖耳并轻微回弹，之后保持明显的警觉姿势
- 思考时左右耳轮流弹一下；开心时连续抖两下再休息，不再只是几乎看不出的微动
- 迷糊状态单耳耷下约 7°，摸摸和睡觉时两耳更明显地向两侧放松
- 待机每隔数秒随机感地轻弹一只耳朵，挥爪时耳朵会同步摆动
- 只调整动作曲线，不修改任何皮肤、分层、流式生命周期或性能限制

## 0.4.1

- 把原定妆图的左右耳分别拆成同画布透明层，脸、眼睛和耳朵花纹保持原画像素
- 听你说时两耳轻轻竖起，迷糊时只耷下一边，开心与挥爪时会小幅抖耳
- 被摸时耳朵放松向两侧，睡觉保留安静的静态软耳姿势
- 隐藏的无耳头毛补层直接烘进身体层，不增加每帧额外绘制；闭眼层仍最后绘制
- 保留 0.4.0 的分层甩尾、Fable51 修稳的正式生成生命周期与手机性能限制

## 0.4.0

- 在 Fable51 修稳的 0.3.6 正式生成生命周期上加入同画布分层尾巴，不改动流式联动判定
- 尾巴绕尾根轻量旋转：待机轻摆，开心快甩，被摸时慢而大，睡觉时几乎不动
- 身体和脸继续使用原定妆图像素，尾巴移开处只用隐藏补毛层填缝，避免多帧素材漂脸
- 保持最高 750 × 750、聊天页 24 FPS、设置页 12 FPS，不使用 Canvas 滤镜或逐帧渐变
- 补齐 0.3.6 延迟结束计时器的销毁清理，热重载不会留下迟到的迷糊状态

## 0.3.6

- 由 Fable51 修正生成生命周期：仅以正式 `GENERATION_STARTED` 开工，忽略 `quiet` 与 `dryRun` 后台生成
- 同时兼容流式“先结束后收信”和非流式“先收信后结束”的事件顺序
- 移除会被后台扩展误触的旧 DOM 兜底，保留发送、停止、回信与切换聊天的明确状态

## 0.3.5

- 参考公开打字指示器验证过的生命周期：流式模式不再被过早到达的 `MESSAGE_RECEIVED` / `GENERATION_ENDED` 骗回待机
- 以 `#mes_stop` 出现作为可靠开工信号，稳定隐藏 520 ms 或 `CHARACTER_MESSAGE_RENDERED` 到达后才结束思考
- 观察发送区内部节点替换，兼容会重新创建发送键 / 停止键的主题与第三方界面
- “让我想想…”在整个生成期间常驻；糯叽会连续轻轻点两下头、停一会儿再继续
- 闭眼眼周素材升级为 v2：保留自然眼皮线，清除灰黑眼圈与烟熏阴影

## 0.3.4

- 打字指示器不再只依赖酒馆消息事件：直接捕获原生 `#send_but` 的手机点击，按下发送即进入思考
- 观察 `#mes_stop` 的显示与隐藏：停止键出现确认正在生成，消失后自动结束思考状态
- 设置面板新增“联动状态”，实机可直接看到最近一次发送或回复信号是否被捕获
- 新增真正闭眼的透明眼周皮肤，只叠加 305 × 190 的小区域，不再用 Canvas 临时画眼皮
- 闭眼素材加载失败时保持原本漂亮的睁眼，不显示任何替代灰块或妖眼
- 保留 0.3.3 的手机性能优化、透明点击穿透和完整生命周期清理

## 0.3.3

- 修复手机端展开糯叽设置时明显卡顿：最高画布由 1000 × 1000 降至 750 × 750，减少约 44% 重绘像素
- 设置预览打开时自动降至 12 FPS，聊天页关闭预览后恢复最高 24 FPS
- 移除会让 Safari 每帧重新光栅化画布的 CSS 阴影，并去掉气泡毛玻璃；画布内原生阴影仍保留
- 闭眼不再绘制两块纯灰椭圆，改为复用糯叽自身毛纹并裁成猫眼形眼皮
- 增接 `USER_MESSAGE_RENDERED` 与 `GENERATION_AFTER_COMMANDS`，用户消息出现后立即进入思考状态
- 加入聊天记录 DOM 观察兜底，兼容绕过部分酒馆事件的第三方输入流程
- 回复到达、停止生成、异常结束和切换聊天时都会清理思考状态的超时保护

## 0.3.2

- 糯叽自己的设置面板展开并处于屏幕内时，桌宠临时提升到移动端抽屉上方
- 收起糯叽设置或关闭扩展抽屉后，自动恢复原本的 `z-index: 3000`
- 强制重置按钮和八个动作按钮横向排字，兼容会压窄 `.menu_button` 的第三方主题
- 保持聊天页层级、透明命中、省电暂停、八种状态和自然眨眼不变

## 0.3.1

- 经 Fable51 工程审校，加入按 Canvas alpha 采样的猫身命中检测
- 透明四角不再挡住酒馆按钮和聊天滚动，点猫时也不会误触底层按钮
- 关闭“让糯叽陪着我”后暂停 24 FPS 动画循环，重新开启时恢复
- 增加停用钩子与统一清理，避免热重载时残留事件和动画
- 保持原有外观、八种状态、自然眨眼和 `z-index: 3000` 不变

## 0.3.0

- 日常陪伴状态加入约 4.7 秒一次的自然眨眼
- 开心、摸摸和睡觉状态会持续温柔闭眼
- 闭眼由 Canvas 在定妆图原眼位绘制，避免多张生图造成脸与身形漂移
- 保留思考点逐个亮起、流式脉冲、倾头和轻跳反馈
- 减少动态效果开启后停止周期眨眼，但保留必要的状态表情

## 0.2.0

- 默认外观更换为透明 2.5D 银白猫狐皮肤
- 保留八种状态的呼吸、倾头、跳跃与轻量特效
- 图片解码完成前保持透明，避免旧造型闪现
- 旧程序造型降级为素材损坏时的应急备用

## 0.1.0

- 首个可玩版本
- 接入 SillyTavern 消息、生成与聊天切换事件
- 支持鼠标 / 触摸拖动、位置保存与移动端安全区域
- 加入扩展设置面板与八种状态预览
